import Vue from 'vue'
import { Checkbox, CheckboxGroup, Progress, Button, Icon, Search, Pagination, Empty  } from 'vant'

Vue.use(Checkbox).use(CheckboxGroup).use(Progress).use(Button).use(Icon).use(Search).use(Pagination).use(Empty)

